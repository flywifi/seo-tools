{
  "_comment": "P94 claim-proof binding (drift invariant 60, tools/sync_check.py::check_claim_proof). A universal claim about this repo's own behavior is a promise, and a promise ships with the executed proof that keeps it. `claims` bind a verbatim sentence to either an enforced drift invariant (`invariant:N`) or a NAMED selftest pin the battery actually runs (`tools/x.py::selftest::<label substring>`). `exempt` records a universal that no code can prove -- a standing instruction to the agent -- with a written reason. `routes` bind an install route the docs RECOMMEND to the code that must DETECT it. `corpus` is the guarded set: a universal claim there that appears in none of the three lists fails the build.",
  "_why": "P93 shipped the commit subject 'no code path installs machine-wide' while the fallback it denied sat on line 138 of the file it changed, and shipped 'every remaining brew install sits under a label' while three live files did not. Both passed every existing guard, because documentation truth checks paths, symbols, counts, URLs and hashes, and a universal sentence names none of those.",
  "corpus": {
    "CLAUDE.md": {
      "section": [
        "## Non-negotiables",
        "## Planning depth"
      ]
    },
    "docs/INSTALL-SCOPE.md": {}
  },
  "claims": [
    {
      "doc": "CLAUDE.md",
      "claim": "No em dashes in user-facing output",
      "proof": "invariant:4",
      "why": "invariant 4 (check_formatting) proves the no-em-dash rule over committed examples/ content, which is the scope the same bullet states; the wider instruction to the agent is covered by the formatting protocol it cites."
    },
    {
      "doc": "CLAUDE.md",
      "claim": "No real CRM data or PII committed to the repo",
      "proof": "invariant:21",
      "why": "check_secret_content reads every tracked text file; invariants 19 and 20 cover the filename and pipeline-allowlist halves."
    },
    {
      "doc": "CLAUDE.md",
      "claim": "Every spoke in the hub's downstream list exists; every atom a workflow names is installed.",
      "proof": "invariant:6",
      "why": "check_hub proves the spoke half and invariant 7 (check_workflows) proves the atom half."
    },
    {
      "doc": "CLAUDE.md",
      "claim": "Every registry entry carries a `default_flag`; drift invariant 53 executes the resolver",
      "proof": "invariant:53",
      "why": "invariant 53 asserts default_flag is present on every entry in the committed connectors.json and executes the resolver over it (P95; before that it only executed the resolver, which reads the field with a fallback and so could never catch a missing one)."
    },
    {
      "doc": "CLAUDE.md",
      "claim": "the code side never writes into a machine-wide site-packages at all",
      "proof": "tools/setup.py::selftest::no .venv: pip is never invoked at all",
      "why": "the P93-4 pin asserts install_dependencies invokes pip zero times when no .venv exists; it fails against the P93-1 code, which installed all seven sets into the base interpreter."
    },
    {
      "doc": "CLAUDE.md",
      "claim": "Drift invariant 59 enforces the labeling across every tracked guidance file",
      "proof": "invariant:59",
      "why": "check_install_scope derives its denominator from git ls-files, so 'every tracked guidance file' is the scanned set rather than a hand list."
    },
    {
      "doc": "CLAUDE.md",
      "claim": "is gated behind the `live_publishing_enabled` flag (default off)",
      "proof": "tools/publishing_compliance.py::selftest::live_publishing_enabled default should be False",
      "why": "the named pin asserts the default directly; the human-confirmation half of that bullet is an agent directive and is exempted separately."
    },
    {
      "doc": "docs/INSTALL-SCOPE.md",
      "claim": "no Creator OS tool ever writes into a machine-wide site-packages",
      "proof": "tools/setup.py::selftest::no .venv: pip is never invoked at all",
      "why": "same executed pin as the CLAUDE.md non-negotiable; this is the policy page's statement of it."
    },
    {
      "doc": "docs/INSTALL-SCOPE.md",
      "claim": "Every remaining `brew install`",
      "proof": "invariant:59",
      "why": "the completeness half of this sentence is exactly what check_install_scope proves over the derived denominator; it was false when the denominator was a 16-file list."
    },
    {
      "doc": "CLAUDE.md",
      "claim": "The drift guard enforces this for `examples/` only.",
      "proof": "invariant:4",
      "why": "this sentence states the enforced scope exactly, and check_formatting is what enforces it."
    },
    {
      "doc": "CLAUDE.md",
      "claim": "nothing under `/Applications`,\n  `/opt/homebrew`, or via `sudo` unless explicitly labeled",
      "proof": "invariant:59",
      "why": "check_install_scope scans every tracked guidance file for exactly these machine-wide forms and fails on any that is unlabeled."
    },
    {
      "doc": "CLAUDE.md",
      "claim": "**Human confirmation required before every post.**",
      "proof": "tools/publishing_compliance.py::selftest::human_review_required must always be True",
      "why": "the named pin asserts the flag is set on every path through the compliance gate."
    },
    {
      "doc": "docs/INSTALL-SCOPE.md",
      "claim": "Nothing goes to `/Applications`, `/opt/homebrew`,\n`/usr/local`, or anywhere `sudo` is needed.",
      "proof": "invariant:59",
      "why": "the same derived-denominator scan proves no unlabeled instruction to install in those locations survives in the guidance."
    },
    {
      "doc": "CLAUDE.md",
      "claim": "`schedule-post` always sets `human_review_required: true`",
      "proof": "tools/publishing_compliance.py::selftest::human_review_required must always be True",
      "why": "the named pin asserts the flag on every path through the gate."
    },
    {
      "doc": "CLAUDE.md",
      "claim": "the repo `.venv` is the only install target",
      "proof": "tools/setup.py::selftest::no .venv: pip is never invoked at all",
      "why": "the pin proves pip is invoked zero times when no .venv exists, so the .venv is the only target the installer ever writes to."
    },
    {
      "doc": "docs/INSTALL-SCOPE.md",
      "claim": "Drift invariant 59\nenforces this over every tracked guidance file (an unlabeled machine-wide instruction fails\nthe build)",
      "proof": "invariant:59",
      "why": "check_install_scope derives its file set from git ls-files and fails on an unlabeled hit."
    },
    {
      "doc": "docs/INSTALL-SCOPE.md",
      "claim": "The repo `.venv` is the only install target, so when it cannot be\ncreated the installer refuses with the remedy instead of falling back to the base interpreter",
      "proof": "tools/setup.py::selftest::no .venv: pip is never invoked at all",
      "why": "the pin proves pip is never invoked without a .venv, which is exactly this sentence."
    },
    {
      "doc": "CLAUDE.md",
      "claim": "when it cannot be created the installer refuses with the remedy rather than falling back to the base interpreter",
      "proof": "tools/setup.py::selftest::no .venv: every requirements set is refused with the user-scoped remedy",
      "why": "the setup selftest removes the .venv path and asserts every requirements set comes back refused with the user-scoped remedy text; its sibling pin asserts pip is never invoked at all."
    },
    {
      "doc": "CLAUDE.md",
      "claim": "advances items to `ready_to_post` for manual posting and makes no network call",
      "proof": "tools/publishing/__init__.py::selftest::flag off: the platform client is never called",
      "why": "dispatch() is the only route from the dashboard to a platform client; the pin swaps in a recording client and observes zero calls with the flag off, and a flag-on control proves the probe sees calls. The dashboard's own flag-off branch sits inline in a 60-second scheduler loop that no selftest drives, so it is covered by this seam rather than pinned directly."
    }
  ],
  "exempt": [
    {
      "doc": "CLAUDE.md",
      "claim": "Never fabricate data, metrics, rates, brands, or sources",
      "why": "A standing instruction to the agent about generated output, not a property of this repo's code. protocols/no-fabrication.md governs it and no guard can prove a negative about future generations."
    },
    {
      "doc": "CLAUDE.md",
      "claim": "Nothing is released until it passes the Quality Gates",
      "why": "A release-process directive routed through the quality-review skill; there is no code path a drift invariant can execute to prove a human followed it."
    },
    {
      "doc": "CLAUDE.md",
      "claim": "Claims about this repo's own behavior meet the same bar as a plan",
      "why": "The claims rule itself, and it quotes the trigger words it governs. Invariant 60 enforces the rule; a self-binding entry would be circular, so it is exempted with this reason instead."
    },
    {
      "doc": "CLAUDE.md",
      "claim": "Installs are user-scoped by default: everything lands under the user's home folder",
      "why": "The policy statement whose two provable halves are bound separately above (invariant 59 for the guidance labeling, the setup.py pin for the code side). This opening sentence is the summary of those bindings."
    },
    {
      "doc": "CLAUDE.md",
      "claim": "Agents never post directly",
      "why": "A behavioral directive about how agents act, enforced by the human-confirmation step in schedule-post rather than by a guard that can execute the negative."
    },
    {
      "doc": "CLAUDE.md",
      "claim": "Those two fields are the only baselines it reads",
      "why": "Describes dependency_currency's read surface, which invariant 25 constrains only at the requirements-versus-registry pin level; the read-surface half is maintainer documentation of the tool's design."
    },
    {
      "doc": "docs/INSTALL-SCOPE.md",
      "claim": "Everything Creator OS asks a person to download or install lands inside their own user",
      "why": "The policy's opening statement of intent; its enforceable halves are bound above to invariant 59 and the setup.py pin rather than duplicated here."
    },
    {
      "doc": "docs/INSTALL-SCOPE.md",
      "claim": "| the repo's `.venv/` | every Python package",
      "why": "An approved-locations table row describing where packages land; the enforceable claim behind it is the setup.py venv-only pin bound above."
    },
    {
      "doc": "docs/INSTALL-SCOPE.md",
      "claim": "caches (pip, Playwright's browser)",
      "why": "A table row naming third-party cache locations those tools choose; this repo does not control them and no guard can assert their behavior."
    },
    {
      "doc": "docs/INSTALL-SCOPE.md",
      "claim": "wizard state, staged bundles, `dist/` builds -- all gitignored",
      "why": "A table row about gitignore coverage, which drift invariant 19 enforces for tracked .local. files; the row itself is descriptive."
    },
    {
      "doc": "docs/INSTALL-SCOPE.md",
      "claim": "nvm, per its README install script",
      "why": "A table row describing a third-party installer's documented behavior; the repo-side obligation (that we can find what it installs) is bound as a route record instead."
    },
    {
      "doc": "docs/INSTALL-SCOPE.md",
      "claim": "already inside the repo `.venv` after `--install-deps`",
      "why": "A table row naming the STT default; the enforceable half is the venv-only install pin bound above."
    },
    {
      "doc": "docs/INSTALL-SCOPE.md",
      "claim": "none needed -- the repository is public",
      "why": "A statement about GitHub repository visibility, verified out of band against the GitHub API in P92-2; no local guard can assert a remote's visibility."
    },
    {
      "doc": "CLAUDE.md",
      "claim": "`shared/connectors/connectors.json` is the source of truth for the connector registry",
      "why": "Names which file is authoritative for maintainers, a convention about where to edit; invariant 53 proves the committed registry resolves, not that no other file claims authority."
    },
    {
      "doc": "CLAUDE.md",
      "claim": "is written only through `tools/registry_io.py`",
      "why": "invariant 42 counts the sanctioned registry writers but is labelled advisory in its own docstring and does not gate the build on a new writer, so this is a maintainer convention backed by an advisory census rather than a proven property."
    },
    {
      "doc": "CLAUDE.md",
      "claim": "is the only tool that writes to `traversal-candidates.json`",
      "why": "no guard inspects traversal-candidates.json or traversal-visited.json at all; this is a maintainer convention with no executed proof behind it, recorded here honestly rather than bound to a check that does not cover it."
    },
    {
      "doc": "CLAUDE.md",
      "claim": "A universal claim (\"no\", \"never\", \"every\", \"all\", \"only\", \"nothing\", \"always\", \"none\", \"cannot\") in a",
      "why": "this sentence QUOTES the trigger vocabulary the rule governs; the words are the subject of the sentence rather than a promise being made."
    },
    {
      "doc": "CLAUDE.md",
      "claim": "drift invariant 60 fails the build when a bound\n  claim drifts from its proof, when a named pin is renamed away, when a recommended install\n  route stops being detectable, or when an unproven universal claim joins the list",
      "why": "invariant 60's own behavior, proven by the fail-then-pass fixtures embedded in check_claim_proof that run on every battery pass through the drift guard rather than by a selftest pin (sync_check is exempt from the selftest sweep because executing it IS its test)."
    },
    {
      "doc": "CLAUDE.md",
      "claim": "is the token-free version-drift checker for pip packages, system\n  binaries, and MCP servers",
      "why": "describes the tool's purpose and read surface for maintainers; invariant 25 constrains only the requirements-versus-registry pin, not the tool's design."
    },
    {
      "doc": "CLAUDE.md",
      "claim": "The resolver (`shared/connectors/connectors.py`) reads this file plus, automatically,",
      "why": "describes which files the resolver consults, a design note for maintainers rather than a promise a guard can execute."
    },
    {
      "doc": "docs/INSTALL-SCOPE.md",
      "claim": "(Microsoft 365 lane only)",
      "why": "scopes which lane needs Node at all; the detectability obligation for that route is bound as a route record instead."
    },
    {
      "doc": "docs/INSTALL-SCOPE.md",
      "claim": "works on every platform, fully user-scoped",
      "why": "describes faster-whisper's own portability, a property of that third-party package rather than of this repo's code."
    },
    {
      "doc": "docs/INSTALL-SCOPE.md",
      "claim": "auth only if a machine must PUSH",
      "why": "states when GitHub credentials are needed, a fact about GitHub's access model that no local guard can assert."
    },
    {
      "doc": "CLAUDE.md",
      "claim": "commit subject, a doc sentence, a CHANGELOG entry, or a report to the owner either names\n  the executed pin that proves it or is narrowed to what was actually tested",
      "why": "the claims rule's own statement of scope, which reaches a chat report to the owner; no guard can read a chat message, so this half is doctrine enforced by judgment rather than by the build."
    },
    {
      "doc": "CLAUDE.md",
      "claim": "Test the PROPERTY\n  claimed, not the mechanism changed",
      "why": "an instruction about how to choose a test, not an assertion about repo behavior that a test could confirm."
    },
    {
      "doc": "CLAUDE.md",
      "claim": "Policy, approved locations, and the one exception (Apple CLT git):",
      "why": "points at the policy page and names the single exception; the enforceable halves are bound separately to invariant 59 and the setup.py pin."
    },
    {
      "doc": "CLAUDE.md",
      "claim": "it queries PyPI and GitHub Releases directly (stdlib, honoring the env proxy + CA bundle) and\n  computes drift from each registry entry's own `validated_version` and `pinned_constraint`",
      "why": "describes how the currency checker works for maintainers; invariant 25 pins only the requirements-versus-registry agreement, not the tool's implementation."
    },
    {
      "doc": "CLAUDE.md",
      "claim": "is consulted only when passed explicitly via `--flags`",
      "why": "describes the resolver's flag-file precedence, a design note; invariant 53 proves the resolver executes over the committed registry, not this precedence rule."
    },
    {
      "doc": "CLAUDE.md",
      "claim": "No connector call is made, and no post is queued or published,\n  without an explicit human confirmation step",
      "why": "restates the human-confirmation promise in negative form; its executed proof is the publishing_compliance pin bound to the same bullet's positive statement."
    },
    {
      "doc": "CLAUDE.md",
      "claim": "they produce\n  confirmation summaries for human review only",
      "why": "a behavioral directive about what agents produce, enforced by the confirmation step rather than by a guard that can execute the negative."
    },
    {
      "doc": "docs/INSTALL-SCOPE.md",
      "claim": "Machine-wide installation is always a labeled\nchoice a person makes deliberately, never a default this repo hands out.",
      "why": "the policy's statement of intent; the enforceable half (that no unlabeled machine-wide instruction survives) is bound to invariant 59 in the following sentence."
    },
    {
      "doc": "docs/INSTALL-SCOPE.md",
      "claim": "written by the apps/tools themselves, always per-user",
      "why": "a table row describing where third-party apps place their own per-user config and caches; this repo neither writes nor controls those locations."
    },
    {
      "doc": "CLAUDE.md",
      "claim": "P93 proved a refusal for PEP 668 interpreters and\n  claimed it for every machine-wide one",
      "why": "a worked example of the mistake the rule exists to prevent, describing a past defect rather than asserting a current property of the code."
    },
    {
      "doc": "CLAUDE.md",
      "claim": "`report`/`check` are read-only",
      "why": "describes the currency checker's two read-only verbs for maintainers; no guard asserts a CLI verb writes nothing."
    },
    {
      "doc": "CLAUDE.md",
      "claim": "so seven requirements sets still installed into a shared\n  site-packages under a commit saying they could not",
      "why": "the second half of the P93 worked example; it recounts what went wrong once, not a property of the code as it stands."
    },
    {
      "doc": "CLAUDE.md",
      "claim": "a new\n  guard's scan set is derived from the tree, never a hand list of the files the change happened\n  to touch",
      "why": "a process precondition for writing a guard, checked by the reviewer at design time; no build step can tell whether a denominator was derived or recalled."
    },
    {
      "doc": "CLAUDE.md",
      "claim": "When a\n  check fails, report it honestly with the output; never claim a skipped step ran.",
      "why": "an instruction governing what the agent says in a report; nothing in the build can read a chat message, so this is doctrine enforced by judgment."
    },
    {
      "doc": "CLAUDE.md",
      "claim": "maintenance runs with no model tokens",
      "why": "Describes how the tool is meant to be run: a deterministic stdlib script from cron or a shell, with no agent in the loop. Spending tokens needs an agent, which is outside anything the script's own code can assert."
    },
    {
      "doc": "docs/INSTALL-SCOPE.md",
      "claim": "no sudo, ever",
      "why": "Paraphrases nvm's own documented per-user install model, quoted from its README in the same cell. A property of a third-party installer that no code in this repo can prove."
    },
    {
      "doc": "docs/INSTALL-SCOPE.md",
      "claim": "sees none of it",
      "why": "The last step of a manual verification procedure the reader performs on their own machine with a second account. It says how to check, not what the code does."
    }
  ],
  "routes": [
    {
      "recommends": "astral.sh/uv/install.sh",
      "in": [
        "docs/INSTALL-SCOPE.md",
        "docs/SETUP_MAC.md",
        "tools/setup.py",
        "tools/wizard.py",
        "Start Creator OS Setup.command"
      ],
      "lands_in": ".local/bin",
      "prober": [
        "tools/env_paths.py::USER_BIN",
        "Start Creator OS Setup.command"
      ],
      "why": "uv installs itself and `uv python install`'s pythonX.Y into ~/.local/bin (uv docs, Installing Python). P93 recommended this route while env_paths searched only the Homebrew prefixes and the launcher probed only machine-wide paths, so the Python its own message told the user to install was invisible."
    },
    {
      "recommends": "nvm-sh/nvm",
      "in": [
        "docs/INSTALL-SCOPE.md",
        "tools/wizard.py"
      ],
      "lands_in": "versions/node",
      "prober": [
        "tools/env_paths.py::NVM_NODE_SUBDIR"
      ],
      "why": "nvm keeps each node under $NVM_DIR/versions/node/<version>/bin (nvm.sh::nvm_version_path) and is sourced per shell. P93 made nvm the recommended Node route while env_paths could not see it, so the wizard looped users on 'Still not detecting Node.js 20 or higher' after issuing its own nvm command."
    }
  ],
  "_detector_branches_why": "Recorded branch names for the two detectors that scan text. Fixture/branch agreement alone only catches a half delete; pinning the names means removing a branch AND its fixture in one edit still fails the build, the realistic way coverage narrows.",
  "detector_branches": {
    "claim_proof": [
      "all",
      "always",
      "bare_no",
      "cannot",
      "every",
      "never",
      "no_ever",
      "none",
      "nothing",
      "only"
    ],
    "install_scope": [
      "brew",
      "macos-pkg",
      "macports",
      "npm-global",
      "pip-command",
      "pip-override",
      "pipx-global",
      "sudo-pkg",
      "sudo-python"
    ]
  }
}
